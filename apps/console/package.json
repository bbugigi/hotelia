{
  "name": "@hotelia/console",
  "version": "0.1.0",
  "private": true,
  "description": "Hotelia Desktop Console — offline-first front desk, housekeeping, POS, messaging, maintenance",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "vite --port 3010 --strictPort",
    "dev:electron": "npm run build:main && cross-env VITE_DEV_URL=http://localhost:3010 electron . --no-sandbox",
    "build": "npm run build:renderer && npm run build:main",
    "build:renderer": "vite build",
    "build:main": "tsc -p tsconfig.main.json",
    "start": "electron . --no-sandbox",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.main.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hotelia/channels": "*",
    "@hotelia/database": "*",
    "@hotelia/events": "*",
    "@hotelia/integrations": "*",
    "@hotelia/shared": "*",
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/cors": "^2.8.0",
    "@types/express": "^4.17.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^4.5.0",
    "cross-env": "^7.0.3",
    "electron": "^36.0.0",
    "typescript": "~5.9.3",
    "vite": "^6.3.0"
  }
}
