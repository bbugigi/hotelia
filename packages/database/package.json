{
  "name": "@hotelia/database",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "deploy": "prisma migrate deploy",
    "seed": "ts-node prisma/seed.ts",
    "studio": "prisma studio",
    "build": "npm run generate && tsc -p tsconfig.cjs.json",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "zod": "^4.5.0"
  },
  "devDependencies": {
    "prisma": "^6.19.3",
    "typescript": "~5.9.3",
    "tsx": "^4.23.13",
    "@types/node": "^22.10.0"
  }
}